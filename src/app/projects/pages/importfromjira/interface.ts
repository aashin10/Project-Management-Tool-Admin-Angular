interface AccessibleResource {
  id: string;
  name: string;
  url: string;
}

interface ImportProjectMinimal {
  name: string;
  key: string;
  id: string;
  selected: boolean;
}

interface CSVUser {
  name: string;
  email: string;
  id: string;
  active: boolean;
}
